# HANDOFF — Market Radar Migration (2026-04-27)

**For:** Next Claude session
**Date:** 2026-04-27 morning
**Status:** Phase A complete, APK rebuild paused, log viewer is next

---

## READ FIRST (in this order)

1. `PROJECT_PATH.md` (project knowledge) — the master path: migration → paper validation → real money
2. `MIGRATION_PLAN.md` (project knowledge) — Phase B-F execution plan
3. `TRADING_VALIDATION_PROTOCOL.md` (project knowledge) — paper-trade discipline + gates
4. This handoff — live state since the project docs were locked

The three docs above are the authoritative path. Do not re-derive or second-guess them. Anything in this handoff that conflicts with them, the docs win.

---

## LIVE STATE (NOT in project knowledge yet)

### Phase A status

- **All 12 directives CLOSED** (A.1, A.2, A.3, A.4, A.4a, A.5, A.6, A.7+A.9+A.10+A.11 bundle v2, A.8)
- **brain.py hash:** `651ceb7b1af6d660ac4bcf63833820ed`
- **Git tag:** `post-a8-2026-04-26`
- **BRAIN_VERSION:** "2.2.15"
- **brain.py line count:** 4835
- **Test files:** 8 suites passing — test_force1_a1.py (7/7), test_force2_a2.py (9/9), test_force3_a3.py (15/15), test_gamma_a4.py (11/11), test_wall_a5.py (26/26), test_context_a6.py (30/30), test_bs_primitives_phaseA.py (38/38), test_chain_interp_a8.py (28/28), test_gate3_structural_counts.py (24/34/24/10 baseline)

### APK rebuild status

**NOT REBUILT YET.** A.8 is committed to repo, tagged, and ready — but Vivek paused the rebuild yesterday (2026-04-26 evening). Decision: bundle the log viewer into the SAME rebuild as A.8, rather than rebuild twice.

### Next action: log viewer directive

Vivek wants an in-app log viewer added before APK rebuild. Reason: he's at office during market hours with only phone, can't access Logcat. The log viewer would let him copy device logs from the app and share them in chat for diagnosis.

**Estimated work:** 4-5 hours, single Antigravity directive.

**Three components needed:**
- Kotlin capture layer (logcat shell-out OR LogTap wrapper around existing Log calls)
- NativeBridge `getLogBuffer()` / `clearLogBuffer()` methods
- MarketVivi UI view (new tab, hidden long-press, OR floating overlay)

### Pending decisions on log viewer (Vivek to answer)

1. **When:** insert NOW (before Phase B), after Phase B closes, or skip entirely
2. **Capture mechanism:** logcat shell-out / LogTap wrapper / both
3. **UI placement:** new tab (5th tab) / hidden long-press on title / floating debug overlay

**Claude's adversary picks (told to Vivek 2026-04-26):** Now + Logcat shell-out + 5th tab. Reasoning: Phase D will need diagnostic visibility; logcat shell-out catches Chaquopy errors without instrumenting Python; 5th tab is findable under stress where long-press would be forgotten.

Vivek dismissed the question last night ("not building APK now, tomorrow we will create this and then make the apk"). New chat should re-ask the three questions or accept Claude's picks if Vivek says "go with your picks."

---

## OPERATING MODE FOR PHASE B-F (locked 2026-04-26)

Phase A's per-directive ceremony was for protocol establishment. Phase B-F runs lighter:

- **Bundle aggressively.** Phase B = single directive covering all 5 decisions (#8 #9 #10 #12 #23). Single test file. Single tracker update.
- **Drop verbatim source-line gates.** Trust regression suite as primary truth-keeper.
- **Skip lock-text amendment gates.** Lock texts already audited end-to-end (45 decisions, GOD_MODE_AUDIT v1.1). If lock text is wrong, real-behavior tests will catch it.
- **One round-trip per directive max.** Halt only on regression failure or scope expansion.
- **Phase D is the exception.** Real-money path warrants per-decision discipline within the phase.

---

## NON-NEGOTIABLES (PROJECT_PATH.md N1-N5)

- N1: No real trades during migration
- N2: No real trades during paper validation until gates pass
- N3: Numerical AND behavioral gates both required
- N4: Migration scope locked at 45 decisions (no scope creep mid-migration)
- N5: Antigravity executes / Claude drafts / Vivek approves — no role contamination

In-app log viewer is justified scope addition because it's diagnostic infrastructure, not trading logic. If Vivek tries to add other "while we're at it" items, push back on N4.

---

## IMMEDIATE NEXT STEPS

1. New chat starts. Vivek pings.
2. Re-ask the 3 log viewer questions OR accept Claude's picks if Vivek says go.
3. Draft the log viewer directive. Single bundled directive — Kotlin capture + NativeBridge + MarketVivi UI.
4. Send to Antigravity. Antigravity executes.
5. APK rebuild includes BOTH A.8 changes AND log viewer.
6. Smoke test on next market open (5 minutes — app launches, watchlist populates, badges render, log viewer works, Logcat clean).
7. If smoke test passes: draft Phase B directive (single bundled directive, decisions #8/#9/#10/#12/#23 + Kotlin setContext payload restructure).

---

## KEY FILE PATHS

- `E:\APP\Marketapp-main` — Antigravity execution target
- brain.py L2945 — `_bs_theta` (extracted during A.8, formula identical to pre-A.8 inline)
- brain.py L3260 — `_interpolate_strike_value` helper (A.8)
- brain.py L3329 — modified `_chain_delta`
- brain.py L3340 — modified `_chain_theta`

---

## KEY UNRESOLVED ITEMS (deferred to post-migration)

- Bull Put 0/6 paper WR vs 91% backtest — investigation deferred to TRADING_VALIDATION_PROTOCOL.md Stage 3 (mandatory)
- Phase 10.G (Upstox Margin/Brokerage/Positions API) — deferred until single source stable
- Calibration tuning — deferred to TRADING_VALIDATION_PROTOCOL.md Stage 4

---

## END HANDOFF
