# G8 / G9 queued work — 2026-09-12

**Status:** **docs only**. No code in this package. Training stays frozen. G9 sizing is **not** adopted.  
**Depends on:** G7 freeze `paper_baseline_g7_20260912_b472` (Marketapp `6ebd206` / 2.6.41 / b472).  
**Plan:** `docs/PLAN_final_implementation_20260912.md` §§12–14.

Do **not** enable `ml_train.run` or `online_update` while building any item below.  
Do **not** remove the live `p_ml` gate.  
Do **not** run G3 `--apply`.  
Do **not** revoke anon INSERT / flip the G1 auth stub.

---

## G8 — repair dormant training without enabling it

Work queue (offline / shadow tooling only). The production freeze remains until a **separate** explicit gate — not this document, not a better accuracy score.

| Item | Defect / required work | Acceptance when later implemented |
|---|---|---|
| **`exportAppTrades` filters** | `MarketMLService.exportAppTrades` still uses `filter = "paper=eq.REAL"` (incompatible with the **boolean** `paper` column) and `order = "date.asc"` (not the trade timestamp schema). | Typed export returns the intended paper/live population; no silent empty/wrong set. |
| **Export coverage** | Independent 1,000-row caps on outcomes and snapshots do not guarantee 1,000 usable primary decisions. | Export by eligible decision/session coverage with deterministic paging and joins. |
| **Net target training** | Replace H2-as-net training conversion with `net_target_v1_gross_minus_costs_once_20260912`. Keep old labels as diagnostics. | Trainer consumes versioned net target; `canonical_won` / H2 remain readable and un-overloaded. |
| **Temporal arity** | Kotlin five-argument temporal call vs Python four-argument function. Need an explicit **real-sequence** path. | Real-sequence fixture reaches the real trainer. Synthetic sequences marked synthetic and **cannot** certify real-path performance. |
| **Split hygiene** | Chronological / session-grouped training; separate probability calibration and threshold selection; untouched test periods. | No future data in features; late outcomes cannot cross boundaries; overlapping candidates are not treated as independent. |
| **Champion / challenger** | Compare both models on the **same** untouched data with the same economics and constraints. Do not compare new validation metadata against an old model's unrelated stored accuracy. | Common test; promotion is an explicit separate step. |
| **Artifacts** | Immutable model artifacts, model cards/manifests, compatibility checks, tested rollback to the last approved artifact (`ml_model.json` SHA `795b3330…` on this freeze). | Incompatible feature schemas reject load. Rollback exercised. |

**Freeze remains.** `RETRAIN_DISABLED_REASON` stays in force. `MLAlarmReceiver` continues to cancel nightly training. `ml_retrain_force_enable` stays default false. Re-enabling training and allowing automatic promotion are **two later decisions**, each requiring their own review bundle.

---

## G9 — queued research, not bundled fixes

### Position sizing — **pending supply**

Claude’s exact patch, commit/base, 627-test list, backtest script, input hashes, and the +38,633 claim were **not supplied** (G0 and still true at G7). Mark **pending**. Do not invent a substitute patch.

When/if supplied, required before any review:

- Reproduce fixed vs variable fee arithmetic separately from the strategy-return claim.
- Test unit/lot conversion, cost caps, per-order effects, minimum lot, margin, concurrent positions, liquidity/slippage, adverse gaps, zero-trade behavior.
- Same data, capital (₹2,50,000), and 10% risk budget for fixed vs proposed sizing.
- Output **shadow-only** first. A user-visible suggestion must be labelled experimental and must **not** silently change quantity or risk limit.
- Increased size must not be used to make weak per-trade economics look robust.
- “627 tests pass” is **not** acceptance.

**Not adopted on the G7 freeze.** Lot count stays 1-lot operating assumption.

### NF / BNF observability

G6 already adds NF/BNF, strategy, DTE, regime, and execution-mode **dimensions** on `ml_evaluation_metrics` with `thin_support` flags. That is measurement, not a behavior change.

Still queued (not in this freeze):

- Index-specific teacher buckets with adequate support and a documented fallback.
- A new ML index feature requires a **new** feature schema and a compatible trained artifact. Do **not** insert it into `ml_feature_schema_v2_1_1_n38` while loading the frozen `2.1.1` model.
- Any BNF construction-rule or ML-gate change is a **separate prospective variant**, not a silent edit to ACTIVE.

Instrument identity / expiry / lot-metadata correctness remains ordinary correctness work (already prefer live `lotSize`).

### DTE-scaled target — archived negative result, no rebuild

Plan §13: do **not** rebuild the DTE-scaled target experiment in this programme. Preserve the reported −7,202 result and its provenance **if supplied**; none was attached at G0/G7.

Keep the distinction: BOOK signal ≠ recorded close ≠ verified broker fill.

---

## G10 pointer — live automation stays off

Current `OrderExecutionService`:

- `EXECUTION_MODE = "SANDBOX"`
- `SANDBOX_BASE_URL = "https://sandbox.upstox.com"`
- Sequential submission does **not** establish successful hedge fills and does not halt safely on every failed child response.

**Live path stays disabled.** A later G10 needs a persistent broker order/position state machine, idempotent timeout recovery, hedge-before-short confirmation, partial-fill / rejected-leg recovery, broker reconciliation, margin/exposure checks, account-level loss limits, independent heartbeat/feed/auth watchdog, and an exercised kill switch.

Those operational gates are independent of G7 paper profitability. Live trading still requires explicit scope and authorization from Vivek. LLM assistants must not become the only mechanism protecting a position.

---

## Explicit non-goals of this note

- Enable training or online update.
- Remove or weaken the live `p_ml` cap.
- Ship or merge a sizing patch that was never supplied.
- Rebuild the DTE-scaled target.
- Cut over Auth or revoke anon INSERT.
- Run G3 peak `--apply`.
